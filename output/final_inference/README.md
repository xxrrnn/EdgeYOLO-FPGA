# Final Inference Outputs

This directory is for user-facing FPGA inference outputs: timing JSON, numeric
results, and rendered detection/classification images.

Large generated files are ignored by git. Keep reusable run commands and model
provenance in the model README or a small manifest.
